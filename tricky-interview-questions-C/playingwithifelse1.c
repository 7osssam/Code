#include <stdio.h>

int main()
{
    int a = 0, b =0;
    if(a == 0)
    if(b == 0)
        printf("hello Mr\n");
    else
        printf("hello Ms\n");
    return 0;
}

//output is hello Mr
// because it is a valid nested if else loop