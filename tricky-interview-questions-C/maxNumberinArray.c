#include <stdio.h>

int main()
{
    int arr[5]= {1,2,98,1715,4};
    int max = arr[0], i;
    for(i = 0; i < arr[i]; i++)
    {
        if(arr[i] > max)
        max = arr[i];
    }
    printf("the max value is %d\n", max);

    return 0;
}